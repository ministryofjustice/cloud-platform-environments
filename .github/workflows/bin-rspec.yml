name: Rspec Tests for bin directory

on:
  pull_request:
    branches:
      - 'main'
    paths:
      - bin
      - lib
      - spec
  push:
    branches:
      - 'main'
    paths:
      - bin
      - lib
      - spec

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - run: echo hello from bin-rspec.yml
