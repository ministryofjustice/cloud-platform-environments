name: Auto-approve a pull request

on:
  push

jobs:
  checksum-compare:
    runs-on: ubuntu-latest
    strategy:
    steps:
      - uses: actions/checkout@v2
      - name: test go
        uses: actions/setup-go@v1
      - run: go run ./bin/auto-approve
