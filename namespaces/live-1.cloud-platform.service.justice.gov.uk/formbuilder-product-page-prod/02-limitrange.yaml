apiVersion: v1
kind: LimitRange
metadata:
  name: limitrange
  namespace: formbuilder-product-page-prod
spec:
  limits:
  - default:
      cpu: 250m
      memory: 500Mi
    defaultRequest:
      cpu: 10m
      memory: 250Mi
    type: Container
