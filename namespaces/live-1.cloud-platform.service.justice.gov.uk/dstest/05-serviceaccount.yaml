apiVersion: v1
kind: ServiceAccount
metadata:
  name: "dstest-svcacct"
  namespace: "dstest"

---
kind: Role
apiVersion: rbac.authorization.k8s.io/v1beta1
metadata:
  name: "dstest-svcacct"
  namespace: "dstest"
rules:
  - apiGroups:
      - ""
    resources:
      - "pods/portforward"
      - "deployment"
      - "secrets"
      - "services"
      - "pods"
    verbs:
      - "patch"
      - "get"
      - "create"
      - "delete"
      - "list"
  - apiGroups:
      - "extensions"
      - "apps"
      - "networking.k8s.io"
    resources:
      - "deployments"
      - "ingresses"
    verbs:
      - "get"
      - "update"
      - "delete"
      - "create"
      - "patch"

---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1beta1
metadata:
  name: "dstest-svcacct"
  namespace: "dstest"
subjects:
- kind: ServiceAccount
  name: "dstest-svcacct"
  namespace: "dstest"
roleRef:
  kind: Role
  name: "dstest-svcacct"
  apiGroup: rbac.authorization.k8s.io
