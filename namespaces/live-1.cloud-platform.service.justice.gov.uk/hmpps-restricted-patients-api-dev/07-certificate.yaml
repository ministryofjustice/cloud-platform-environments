apiVersion: cert-manager.io/v1alpha3
kind: Certificate
metadata:
  name: dev.restricted-patients-api.service.justice.gov.uk
  namespace: hmpps-restricted-patients-api-dev
spec:
  secretName: restricted-patients-api-cert
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
  - dev.restricted-patients-api.service.justice.gov.uk
