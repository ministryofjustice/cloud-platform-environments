apiVersion: v1
kind: LimitRange
metadata:
  name: limitrange
  namespace: cccd-dev
spec:
  limits:
  - default:
      cpu: 100m
      memory: 2Gi
    defaultRequest:
      cpu: 10m
      memory: 1000Mi
    type: Container
