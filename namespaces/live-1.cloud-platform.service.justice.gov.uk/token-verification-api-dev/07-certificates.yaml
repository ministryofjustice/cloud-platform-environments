---
apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: token-verification-dev.service.justice.gov.uk
  namespace: token-verification-dev
spec:
  secretName: token-verification-cert
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: 'token-verification-dev.service.justice.gov.uk'
  acme:
    config:
    - domains:
      - 'token-verification-dev.service.justice.gov.uk'
      dns01:
        provider: route53-cloud-platform
