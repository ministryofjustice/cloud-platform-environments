apiVersion: cert-manager.io/v1alpha3
kind: Certificate
metadata:
  name: prisoner-content-hub-production-nprrelay-certificate
  namespace: prisoner-content-hub-production
spec:
  secretName: prisoner-content-hub-nprrelay-certificate
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
  - nprrelay.content-hub.prisoner.service.justice.gov.uk
