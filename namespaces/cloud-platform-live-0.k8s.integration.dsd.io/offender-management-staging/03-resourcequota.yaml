apiVersion: v1
kind: ResourceQuota
metadata:
  name: namespace-quota
  namespace: offender-management-staging
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "10"
    limits.memory: 20Gi
