apiVersion: v1
kind: LimitRange
metadata:
  name: limitrange
  namespace: formbuilder-publisher-test
spec:
  limits:
  - default:
      cpu: 250m
      memory: 300Mi
    defaultRequest:
      cpu: 100m
      memory: 128Mi
    type: Container
