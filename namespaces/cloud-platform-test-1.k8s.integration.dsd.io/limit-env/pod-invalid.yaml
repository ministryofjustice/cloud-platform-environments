apiVersion: v1
kind: Pod
metadata:
  name: pod-invalid
spec:
  containers:
  - name: kubernetes-serve-hostname
    image: gcr.io/google_containers/serve_hostname
    resources:
      limits:
        cpu: 400m
        memory: 100Mi