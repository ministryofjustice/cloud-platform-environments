apiVersion: v1
kind: ResourceQuota
metadata:
  name: namespace-quota
  namespace: limit-env
spec:
  hard:
    requests.cpu: 1000m
    requests.memory: 1Gi
    limits.cpu: 2000m
    limits.memory: 2Gi
