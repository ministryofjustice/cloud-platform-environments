apiVersion: v1
kind: LimitRange
metadata:
  name: limitrange
  namespace: dstest-gitops
spec:
  limits:
  - default:
      cpu: 1000m
      memory: 1000Mi
    defaultRequest:
      cpu: 15m
      memory: 10Mi
    type: Container
