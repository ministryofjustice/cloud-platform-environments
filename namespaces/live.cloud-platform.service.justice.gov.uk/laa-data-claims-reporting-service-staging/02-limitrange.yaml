apiVersion: v1
kind: LimitRange
metadata:
  name: limitrange
  namespace: laa-data-claims-reporting-service-staging
spec:
  limits:
    - default:
        cpu: 2
        memory: 8Gi
      defaultRequest:
        cpu: 1
        memory: 4Gi
      type: Container
