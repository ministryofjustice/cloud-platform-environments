apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: laa-fee-scheme-api-service-monitor
  namespace: laa-fee-scheme-api-dev
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: laa-fee-scheme-api-dev
  namespaceSelector:
    matchNames:
      - laa-fee-scheme-api-dev
  endpoints:
    - port: actuator
      interval: 15s