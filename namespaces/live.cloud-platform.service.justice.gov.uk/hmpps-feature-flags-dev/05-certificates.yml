apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: feature-flags-cert
  namespace: hmpps-feature-flags-dev
spec:
  secretName: hmpps-feature-flags-cert
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - feature-toggles-dev.hmpps.service.justice.gov.uk
