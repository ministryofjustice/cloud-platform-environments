apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: feature-flags-cert
  namespace: hmpps-feature-flags-dev
spec:
  secretName: hmpps-sentence-plan-ui-cert
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - feature-flags-temp-dev.hmpps.service.justice.gov.uk
