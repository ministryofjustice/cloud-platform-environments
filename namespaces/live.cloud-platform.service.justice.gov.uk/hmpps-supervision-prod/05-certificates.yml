apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: supervision-packages-api
  namespace: hmpps-supervision-prod
spec:
  secretName: supervision-packages-api-cert
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - supervision-packages-api.hmpps.service.justice.gov.uk