apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: reuse-library-dev-certificate
  namespace: reuse-library-dev
spec:
  secretName: reuse-library-cert
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
  - dev.reuselibrary.service.justice.gov.uk
