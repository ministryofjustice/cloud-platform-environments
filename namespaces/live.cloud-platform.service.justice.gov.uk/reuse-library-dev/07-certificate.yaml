apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: reuse-library-cert
  namespace: reuse-library-dev
spec:
  secretName: reuse-library-secret
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - dev.reuselibrary.service.justice.gov.uk
