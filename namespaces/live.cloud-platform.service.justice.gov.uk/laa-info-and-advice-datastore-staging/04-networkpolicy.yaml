apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: laa-info-and-advice-datastore-staging
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
