apiVersion: v1
kind: LimitRange
metadata:
  name: limitrange
  namespace: migration-link-exchange-prod
spec:
  limits:
  - default:
      cpu: 8000m
      memory: 8000Mi
    defaultRequest:
      cpu: 100m
      memory: 500Mi
    type: Container
