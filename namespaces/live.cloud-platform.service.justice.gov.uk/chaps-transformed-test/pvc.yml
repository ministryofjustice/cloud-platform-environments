apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chaps-transform-dpkeys-pvc
  namespace: ${KUBE_NAMESPACE}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: gp2