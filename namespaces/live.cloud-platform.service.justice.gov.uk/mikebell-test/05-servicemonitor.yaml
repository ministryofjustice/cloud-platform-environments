apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: example-service
  namespace: mikebell-test
spec:
  selector:
    matchLabels:
      app: example-service
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
