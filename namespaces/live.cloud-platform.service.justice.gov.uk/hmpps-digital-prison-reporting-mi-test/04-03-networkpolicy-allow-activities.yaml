kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: allow-hmpps-activities-management
  namespace: hmpps-digital-prison-reporting-mi-test
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: "hmpps-activities-management-dev"
