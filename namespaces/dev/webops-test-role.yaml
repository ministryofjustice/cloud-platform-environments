kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  namespace: dev
  name: webops-test-role
rules:
- apiGroups: ["rbac.authorization.k8s.io"]
  resources: ["pods", "nodes", "namespaces", "deployments"]
  verbs: ["get", "watch", "list"]
